/**
 * Campfire Widget - Integration Module
 * 
 * Exports integration helpers for gradual migration.
 */

const UserManagerBridge = require('./UserManagerBridge');

module.exports = {
  UserManagerBridge
};
